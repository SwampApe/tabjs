import React from 'react';

//Placeholder to be converted to a Tab component when a new Tab is created

function TabDefault(props) {
    return ( 
        <>
        </>
    );
}

export default TabDefault